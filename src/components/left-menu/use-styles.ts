import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  list: {
    overflow: 'auto',
    width: 250,
  },
  paperRoot: {
    marginTop: 64,
    background: 'transparent',
    border: 'none',
  },
})
